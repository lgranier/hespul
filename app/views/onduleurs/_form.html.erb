<%= form_for(onduleur) do |f| %>
  <% if onduleur.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(onduleur.errors.count, "error") %> prohibited this onduleur from being saved:</h2>

      <ul>
      <% onduleur.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lb %>
    <%= f.text_field :lb %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
